import createTheme from "spectacle/lib/themes/default";

const colors = {
  primary: "#007ACC",
  secondary: "white",
  tertiary: "white",
  quaternary: "white",
};

const fonts = {
  primary: "monospace",
  secondary: "monospace",
  tertiary: "monospace",
};

export const theme = createTheme(colors, fonts);
